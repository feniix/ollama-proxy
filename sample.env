# Ollama Proxy Sample Environment Configuration

# ===== Server Configuration =====
# Host address to bind the proxy server to
# Use 0.0.0.0 to listen on all interfaces, or 127.0.0.1 for local access only
API_HOST=127.0.0.1

# Port for the proxy server to listen on
API_PORT=11435

# ===== Security Configuration =====
# API key for authenticating requests to the proxy
# IMPORTANT: Change this to a strong, unique value
API_KEY=your-secure-api-key-here

# ===== SSL Configuration (Optional) =====
# Path to SSL certificate file for HTTPS
# Comment out or remove if not using SSL
# API_SSL_CERTFILE="./path/to/fullchain.pem"

# Path to SSL key file for HTTPS
# Comment out or remove if not using SSL
# API_SSL_KEYFILE="./path/to/privkey.pem"

# ===== Ollama Configuration =====
# URL of the Ollama server to proxy requests to
# Default is the standard Ollama local server
OLLAMA_URL=http://localhost:11434

# ===== Optional Client Configuration =====
# Domain name for the proxy server (for client.py)
SSL_DOMAIN=your-domain.com

